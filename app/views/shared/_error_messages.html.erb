<% if this.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(this.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% this.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>	